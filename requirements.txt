# repo root mein chalo (jahan frontend/ backend/ package.json waq.) 
cd /path/to/your/local/InvoiceGenie   # <-- replace karo agar zaroori ho

# Create requirements.txt (exact versions taken from pyproject)
cat > requirements.txt << 'EOF'
flask>=3.1.2
flask-cors>=6.0.1
pandas>=2.3.3
scikit-learn>=1.7.2
textblob>=0.19.0
gunicorn>=20.1.0
EOF
